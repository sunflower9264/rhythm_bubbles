Original prompt: 重写 Rhythm Bubbles，保持核心玩法不变，全面重做 UI/UX，所有图片素材由 Codex 生成；使用可在服务器纯代码开发的 H5 技术栈，风格可爱，原创生成音效，最终达到可直接上线的生产级质量。

## Decisions

- Stack: Phaser 3.90 + TypeScript 5.9 + Vite 7.
- Form factor: mobile-first portrait H5, centered and framed on desktop.
- Visual direction: cute bubble cloud garden; generated art only, no third-party image assets.
- Audio direction: deterministic procedural WAV generation committed with its source script.
- Gameplay invariant: one run automatically progresses through classic / memory / sequence; every target clears in one valid hit for +10 scoring, mistakes cost shield/health, timeout causes a heavy hit and refreshes the board, and only zero health ends the run.
- Battle loop: correct bubbles deal combo damage while enemies charge attacks in real time; combos and board clears push the charge meter back without hard-cancelling the attack. Preview, pause, transition, and reward selection freeze the attack clock. Four reward choices rotate through three-option drafts; battle five is the boss and ends in victory.

## Status

- [x] Inspected and traced the original Cocos Creator gameplay.
- [x] Selected the server-friendly web stack.
- [x] Generated original background and app-icon source art.
- [x] Rebuild core rules and tests.
- [x] Rebuild Phaser presentation and responsive DOM UI.
- [x] Generate audio assets.
- [x] Run browser interaction, screenshot, mobile and production-build validation.
- [x] Run the production flow on Chromium, Firefox, and WebKit with no console errors.
- [x] Visually inspect mobile menu, gameplay, sequence preview, results, and desktop layout screenshots.
- [x] Clear transient score particles and level toasts when a new mode starts, then rerun all browser checks.
- [x] Add the complete five-battle Roguelite loop, original enemy art, combat HUD, reward draft, boss, victory state, and battle audio.

## Notes

- The legacy Cocos project was removed from the working tree after the web build passed validation; it remains recoverable from Git history.
- Final checks: 5/5 core tests pass, TypeScript/Vite production build passes, `npm audit --omit=dev` reports 0 vulnerabilities, and `git diff --check` passes.
- The workspace Skill client cannot resolve the project's Playwright dependency from its own `.codex/skills/` path. The identical project-local client at `scripts/web-game-playwright-client.mjs` was used successfully; its final state and screenshot are in ignored `output/web-game-final/`.
- Git push and HTTPS/domain configuration remain intentionally undone; the current deployment uses the machine's public IP over HTTP.
- Deployed the current production build through the machine's Nginx on port `18088`, with files in `/var/www/rhythm-bubbles` and the versioned site config in `deploy/nginx/rhythm-bubbles.conf`. Public-IP Chromium E2E passed after deployment.
- Replaced the original 96 BPM background loop with `bubble-garden-groove-v2.wav`: a 120 BPM eight-bar loop using four-on-the-floor kick, backbeat percussion, eighth-note hats, pulsing bass, arpeggios, and a brighter melodic hook. The 16-second WAV has no clipped samples and a zero-delta loop boundary. Service Worker cache moved to `v3`, Nginx now serves WAV as `audio/wav`, and the public-IP Chromium E2E passed after redeployment.
- Rebuilt click feedback as a jelly-pop sequence: synchronous squash, delayed membrane release, pooled droplets, expanding rings, neighbour waves, three rotating correct-pop sounds, and a soft wrong wobble with a delayed results modal. Wrong feedback now cancels pending positive callbacks so rapid input cannot mix `+10` with failure feedback. Haptics were shortened, reduced-motion bypasses the long reveal, Service Worker cache moved to `v4`, all three browser engines passed, and the Nginx deployment was verified through the public IP.
- Removed desktop-oriented keyboard navigation, `F`/Escape shortcuts, keyboard focus rings, and the in-app fullscreen control. The game now presents touch as its formal mobile-H5 input while retaining pointer-click compatibility; Service Worker cache moved to `v5`. Core tests, build, audit, Chromium/Firefox/WebKit production flows, Nginx file parity, and the public-IP Chromium flow all passed after redeployment.
- Raised the default background-music volume from 20% to 40% to match the main sound effects, added a live 0–100% music-volume slider with local persistence, and moved the Service Worker cache to `v6`. Core tests, build, audit, and Chromium/Firefox/WebKit flows passed; the mobile settings layout was visually inspected at 390 × 844.
- Added a five-encounter battle loop without replacing bubble tapping: correct bubbles retain +10 scoring and now deal escalating combo damage; an enemy that survives a cleared board counterattacks; each of the first four wins offers three rotating upgrades; battle five is a boss with a dedicated victory ending. Added generated jelly-enemy art, enemy/player health HUD, shield and combo indicators, reward cards, battle/victory sounds, and full battle state in `render_game_to_text()`. Seven core tests (including all three modes across 24 procedural seeds) and the complete Chromium/Firefox/WebKit flow passed; mobile screenshots for normal combat, enemy attack, rewards, boss, victory, and failure were inspected after fixing an enemy/prompt overlap. Service Worker cache moved to `v7`; Nginx file parity, config validation, and the public-IP complete Chromium flow passed after deployment.
- Moved the bubble board 50 logical pixels downward and the enemy 35 pixels downward, creating separate vertical bands for instructions, the monster, and the playable board; updated touch-coordinate automation to match.
- Rebalanced the battle curve for launch tuning: encounter HP/attack now rise 42/8 → 175/20, power is +3, heart is +12/+18 heal, shield is 20, and time is +4s/+14 heal; expanded automated winnability coverage to 3 modes × 4 reward routes × 64 seeds. Moved the board to 920 and enemy to 525 to increase prompt-to-monster separation while keeping the board inside the portrait safe area.
- Changed enemy defeat to an uninterrupted clear-board finisher: reaching 0 HP early enters a visible broken-enemy chase state, remaining targets stay playable and continue scoring, and rewards/victory appear only after the final target. The last hit now layers full-canvas camera shake and flash, enemy recoil, a 42-droplet burst, stronger audio/haptics, and a brief shell-wide impact while respecting reduced-motion; the old border-only board shake was removed.
- Replaced board-clear counterattacks with real-time enemy combat. Each encounter now has an escalating charge/windup/recovery cadence; windup telegraphs a full-screen monster collision, killing the enemy during windup interrupts it, and all non-playing decision/preview states freeze the clock. Mistakes now reset combo, advance enemy pressure, and cost shield/health without ending the run; timeout causes a 1.5× screen impact and refreshes the board; only zero health fails. Added an attack-intent HUD, shell-wide impact feedback, generated collision audio, dynamic reward descriptions, 12 core tests, and production E2E coverage for charging, impact, interruption, mistake continuation, timeout recovery, frozen rewards, all modes, Boss victory, and fatal failure across Chromium, Firefox, and WebKit.
- Reworked mistake control and defensive skill expression: each board now tolerates three mistakes, visibly tracks `0/3` through `3/3`, and the fourth mistake applies its normal damage before replacing the board. Replaced the one-click hard interrupt with soft charge reduction: a combo pushes the attack meter back 14%, a final-bubble clear pushes it back 26%, and a combined trigger pushes it back 40%; the enemy then resumes charging. Added readable meter recoil, stagger feedback, 15 core tests, and E2E coverage for both mechanics.
- Fixed two presentation regressions and added a fighting-game Combo readout. Player health had updated in state and inline CSS but could remain visually full in mobile rendering; health width is now updated directly and deterministically, with a short number/track damage pulse. Rapid combo charge reductions had reused `camera.flash`, causing repeated whole-screen flicker; routine combo flashes are now forbidden and only monster impacts/finishers retain them. Added a right-edge 2+ HIT COMBO burst with punch scaling and 5/8-hit color tiers, plus E2E assertions for actual health-bar pixel ratio, zero routine combo flashes, Combo appearance, and mistake reset.
- Removed the 90ms full-screen flash from the board-clearing finisher after confirming it caused the reported end-of-fight flicker. Enemy recoil, camera shake, droplet burst, exit tween, finisher text, sound, and haptics remain intact; production E2E now asserts that finishers emit zero camera flashes.
- Reduced whole-screen motion after an audit found 32 routine correct-hit shakes plus duplicated finisher/monster-impact layers in one five-battle run. Routine correct clicks, mistakes, finishers, monster impacts, and timeout impacts now rely on local/UI feedback and the DOM shell impact where appropriate; only the rare 25%+ charge-reduction stagger keeps a short camera shake.
- Deployed the reduced-shake build to Nginx port `18088` with Service Worker cache `v13`. The deployed files match `dist/`, the public endpoint serves the new cache version, and the complete public-IP Chromium flow passed after deployment; local Chromium, Firefox, and WebKit flows had already passed on the same build.
- Replaced the three selectable mode cards with one bottom-safe “开始游戏” entry and a fixed five-stage learning curve: 寻光 → 记忆 → 旋律 → 二重奏 → 三重奏 Boss. Battles four and five add deterministic 2-hit and 2/3-hit target bubbles without advancing the melody cursor until all layers are broken. Rebuilt 紫莓果冻 as the first interactive encounter: damage and time trigger ordered tentacle locks on the current board, off-order counters consume mistake tolerance, two successful counters break poise, and the stagger window grants 1.5× damage; below half health the intent expands from two to three marks. Added state/text automation fields, intent tethers, layer badges, 12 focused logic tests across 96 seeded reward routes, and a rewritten production browser flow. Service Worker cache moved to `v14`; deployment was intentionally not changed in this iteration.
- Deployed the single-route / 紫莓果冻 counterplay build to the existing Nginx site on port `18088`. `dist/` and `/var/www/rhythm-bubbles/` match exactly, Nginx configuration and reload passed, the public endpoint serves Service Worker cache `v14`, and the complete public-IP Chromium flow passed through victory, failure, settings persistence, and responsive layout. The deployed stagger screenshot was visually inspected.
- Simplified the post-melody difficulty curve: battles 3–5 now remain the same sequence mode, with no “重奏/狂想” mode escalation. Battles 4 and 5 generate exactly one 2-hit target per board and never create 3-hit or multiple armored targets. Updated menu/HUD language, deterministic tests, browser assertions, README/AGENTS guidance, and prepared Service Worker cache `v15`; this change is not deployed yet.
- Deployed the simplified melody curve to Nginx port `18088`. The public endpoint serves cache `v15`; deployed files match `dist/`, the complete public Chromium flow passed, and the fifth-battle screenshot confirms exactly one visible remaining layer (`×1`) with no triple-hit bubbles.
- Simplified the home screen to one mid-lower “开始游戏” button followed by “游戏设置”; removed the route preview, explanatory copy, top settings icon, and the canceled game-help entry/modal. Deleted every generated tutorial-image intermediate and bumped the Service Worker cache to `v16`. Core tests, build, audit, patch checks, and local Chromium/Firefox/WebKit production flows passed; the 390 × 844 menu screenshot was visually inspected.
- Fixed the result modal showing `[object Object]` in its rightmost best-score field after a reload. The generic storage reader had object-spread a persisted numeric score into `{}`; it now validates and returns finite numeric values directly while retaining object merging for preferences. Added unit and browser reload regressions, deployed Service Worker cache `v17`, and verified the public result field remains `450` after reload plus the complete Chromium flow.
- Removed the double-hit bubble mechanic from battles 4 and 5. All sequence targets now clear and advance on one correct tap; removed layer counters, layer-hit presentation, extra-hit timing, double-hit prompts, and serialized hit fields. Focused unit tests plus local Chromium/Firefox/WebKit flows passed, the `v18` build was deployed with exact file parity, and the complete public Chromium flow and Boss screenshot passed.
- Rebalanced combat around sustained encounters and fair mistakes: enemy HP now rises `110/120/170/240/340`, normal combo charge reduction fell from 14% to 4%, board-clear reduction from 26% to 10% (14% combined), and mistake damage now rises gently at `5/5/6/7/8` while full enemy attacks remain unchanged. Added exact curve/reduction tests and per-battle browser assertions; 96 seeded reward-route simulations and local Chromium/Firefox/WebKit production flows passed. Deployed cache `v19` with exact file parity; the complete public Chromium flow and the `327/340` Boss screenshot passed.
- Replaced the first enemy's straight intent links with animated octopus tentacles matching 紫莓果冻: layered curved limbs, alternating suckers, active-tentacle highlighting, and two pulsing constriction rings that visibly bind each target bubble. The animation is presentation-only, remains touch-transparent, becomes static under reduced motion, and exposes rendered-count/phase diagnostics for E2E. All 14 tests and local Chromium/Firefox/WebKit flows passed; cache `v20` was deployed with exact file parity, and the public Chromium flow plus live tentacle screenshot passed.
- Increased the five-enemy HP curve to `150 / 240 / 380 / 600 / 950`, lowered base player attack to `8`, reduced combo bonus cap to `+3`, and reduced power rewards to `+2`. Added a persistent translucent shield field, partial-hit ripple, full shield fracture shards, deterministic original `shield-break.wav`, and shield diagnostics. First-battle damage and timeout attacks now use unified purple tentacle lashes; the monster body and DOM shell no longer perform the screen collision in battle one. Updated browser assertions and visually inspected screenshots for tentacle impact, partial shield block, shield break, Combo placement, and Boss HP. All 15 core tests, build, audit, diff checks, and local Chromium/Firefox/WebKit production flows pass. Service Worker cache prepared as `v21`; deployment pending.
- Refined the first enemy feedback after visual review: all tentacle/lock colors now use a single coral-red rage palette matching the enemy's red windup tint; first-battle impact is a perspective near-camera sucker strike with expanding red screen veil, crack lines, flash, and camera/exterior shell impact, while the enemy body stays in place. Timeout attacks use the same tentacle strike language. Service Worker cache moved to `v22`; core/build/audit and Chromium/Firefox/WebKit flows pass, screenshots inspected, Nginx deployment has exact file parity, and the complete public-IP Chromium flow passes.
- Replaced the first enemy's oversized sucker-first impact with a cinematic heavy-limb strike: a thick tapered tentacle now sweeps across the playfield, while small suckers remain only as inner-surface detail. Attack startup clears both bubble-binding layers, an explicit `tentacleImpactActive` gate prevents them from being redrawn during impact/recovery, and E2E asserts zero rendered bindings throughout the strike. The isolated gameplay frame was visually inspected at `output/e2e/chromium/04-jelly-devour-impact.png`; cache is prepared as `v23`, but this version is not deployed pending visual confirmation.
- Unified the first enemy's bubble-binding and screen-strike tentacles around the same coral three-layer outline, tapered silhouette, and alternating outlined sucker motif. The strike remains heavier through scale and impact feedback without the previous oversized mid-limb bulge; timing, damage, and binding/strike mutual exclusion are unchanged. Service Worker cache is prepared as `v24`; all 15 logic tests, production build, audit, and Chromium/Firefox/WebKit flows pass, and both intent/impact frames were visually inspected. Deployment remains pending.
- Deployed the v24 unified tentacle build to the existing Nginx site on port `18088`. `dist/` and `/var/www/rhythm-bubbles/` match exactly, Nginx validation and reload passed, the local and public endpoints serve Service Worker cache `v24`, and the complete Chromium production flow passed against both endpoints.
- Replaced the first enemy's bubble lock rings with a continuous two-turn binding coil. Each coil alternates rear segments below the bubble sprite and diagonal front segments above it, with front-facing suckers and a subtle constriction pulse, so the limb visibly passes around the bubble instead of drawing complete ellipses over it. Service Worker cache is prepared as `v25`; all 15 logic tests, build, audit, project-local Playwright client, and Chromium/Firefox/WebKit flows pass, and the final binding frames were visually inspected. Deployment remains pending.
- Re-anchored the first enemy's screen attacks to the jelly's lower side lobes after visual review. Normal strikes now emerge from the right lower lobe and timeout strikes symmetrically from both lower lobes; the first 14% of each limb renders behind the enemy while the extended strike crosses into the foreground, and the root taper was reduced to keep the face unobstructed. Added a timeout strike screenshot assertion. Service Worker cache is prepared as `v26`; all 15 logic tests, build, audit, project-local Playwright client, and Chromium/Firefox/WebKit flows pass, with ordinary and timeout attack frames visually inspected. Deployment remains pending.
- Deployed the v26 binding-coil and anatomically anchored screen-strike build to the existing Nginx site on port `18088`. `dist/` and `/var/www/rhythm-bubbles/` match exactly, Nginx validation and reload passed, the public endpoint serves Service Worker cache `v26`, and the complete public Chromium production flow passed including ordinary and timeout tentacle attacks.
- Removed both tentacle presentation systems at the user's request while preserving the first enemy's ordered counterplay. Windup now uses simple straight links from the enemy to numbered targets, and failed counters/timeouts reuse the common monster-body screen collision with scale, flash, shell impact, and recovery. Removed all tentacle graphics, animation helpers, impact gates, and diagnostics; automation now asserts rendered intent links and the enemy body's impact position/scale. Service Worker cache is prepared as `v27`; all 15 logic tests, build, audit, project-local Playwright client, and Chromium/Firefox/WebKit flows pass, with link and body-impact frames visually inspected. Deployment remains pending.
- Deployed the v27 straight-link and monster-body collision build to the existing Nginx site on port `18088`. `dist/` and `/var/www/rhythm-bubbles/` match exactly, Nginx validation and reload passed, the public endpoint serves Service Worker cache `v27`, and the complete public Chromium production flow passed with no tentacle presentation remaining.
- Added persistent progressive shield damage for the translucent player shield: intact, light, damaged, and critical stages now retain increasingly dense branching cracks according to the remaining shield ratio; depletion expands the crack web, emits shards, and removes the shield. Added shield max/ratio/stage automation fields plus intact, impact, damaged, and break E2E captures. All 15 logic tests, build, audit, project-local Playwright client, and Chromium/Firefox/WebKit production flows pass; the intact, 30%-remaining, and break screenshots were visually inspected. Service Worker cache is prepared as `v28`; deployment pending.
- Deployed the v28 progressive translucent shield-damage build to the existing Nginx site on port `18088`. `dist/` and `/var/www/rhythm-bubbles/` match exactly, Nginx validation and reload passed, the public endpoint serves Service Worker cache `v28`, and the complete public-IP Chromium flow passed through shield intact/impact/damaged/break checks, victory, failure, settings, and responsive layout.
- Fixed the first enemy's partially resolved intent presentation without changing counterplay rules: link rendering and numbered markers now start at `enemyIntentCursor`, so a resolved line disappears immediately, and counter bubbles that are not logically cleared restore after their pop feedback instead of remaining invisible. Added a mid-intent browser assertion and `03b-jelly-intent-resolved.png` capture. All 15 logic tests, build, audit, project-local Playwright client, and Chromium/Firefox/WebKit production flows pass; the one-remaining-link frame was visually inspected. Service Worker cache is prepared as `v29`; deployment pending.
- Deployed the v29 resolved-intent link fix to the existing Nginx site on port `18088`. `dist/` and `/var/www/rhythm-bubbles/` match exactly, Nginx validation and reload passed, the public endpoint serves Service Worker cache `v29`, and the complete public-IP Chromium flow passed including the new partial-intent regression.
- 缩短 Combo 衔接窗口至 1 秒（原 1.2 秒），同步核心测试、生产 E2E 断言与 README。护盾常态改为覆盖整个游戏画布的半透明白色护罩；损伤裂纹移入受击时的短暂独立反馈层，非受击状态不再持续绘制破损，归零时仍保留碎裂碎片效果。15 项逻辑测试、生产构建、依赖审计、项目本地 Playwright 客户端和 Chromium 完整生产流程通过；已目视检查护盾完整、受击、反馈退场及碎裂截图，未部署。
- 参考 Pac-Man 的差异化追踪人格、Galaga 的捕获/救援、Punch-Out!! 的预警与破解窗口、Zelda Boss 的“观察—破解—反击”结构和 Space Invaders 的半血加速压力，完成五只随机无重复海洋怪物：保留紫莓果冻顺序对招，新增灯笼骗骗鱼救援牵引、铠潮寄居蟹双弱点破壳、星翼魔鬼鱼危险行扫线、泡泡刺豚停手反击与半血加速。用内置 imagegen 生成并抠出四张透明怪物图，接入专属 UI 文案和盘面提示；Service Worker 缓存更新为 `v30`。20 项逻辑测试、构建和本地 Chromium/Firefox/WebKit 完整生产流程通过，四只新怪常态与特色攻击截图均已目视检查；未部署。
- 已将五只随机海洋怪与 Combo/全屏半透明白色护盾版本部署到现有 Nginx 站点 `http://64.83.41.39:18088`。`dist/` 与 `/var/www/rhythm-bubbles/` 完全一致，Nginx 配置校验和重载通过，公开端点返回 Service Worker `v30`；线上 Chromium 完整五战流程通过，覆盖随机顺序、五种反制、护盾、胜利、设置和响应式布局。
- 将普通连击和清盘对怪物蓄力的回退分别从 `4% / 10%` 降为 `0.5% / 0.5%`，连击清盘合计仅 `1%`；新增约每秒 7 次有效点击仍能让怪物在存活时进入 `windup` 的回归测试。当前 22 项逻辑测试、TypeScript 检查和生产构建均通过。
- 将记忆预览改为全部目标只显示一次、持续 900ms；旋律预览改为所有编号目标同时显示，并按目标数量停留约 1.2–2.1 秒。倒计时、敌人生命、怪物蓄力、玩家生命、Combo 窗口及音乐音量条统一加入流动液体高光与微泡粒子，`reducedMotion` 和系统减少动态时保持静态。Service Worker 缓存准备更新为 `v31`；等待完整浏览器验证、提交、推送和部署。
- 已提交并推送预览、蓄力平衡和液体数值条版本，并部署到现有 Nginx 站点 `http://64.83.41.39:18088`。`dist/` 与 `/var/www/rhythm-bubbles/` 完全一致，Nginx 校验和重载通过，公开端点返回 Service Worker `v31`；线上 Chromium 完整五战流程通过，最终自动化竞态修正提交为 `e75a5cd`。
- 按反馈重写数值条视觉：移除轨道外围横向漂移粒子，把倒计时、敌我生命、怪物蓄力和 Combo 改为与当前填充值严格同步的药剂槽；药剂内部气泡纵向上升，末端弯月高光随数值位置移动，空槽不再显示粒子。音乐音量条同步移除外围粒子并改为填充范围内的气泡纹理；保留 reduced-motion 静态模式。22 项逻辑测试、生产构建、依赖审计、项目本地 Playwright 客户端和 Chromium/Firefox/WebKit 完整生产流程均通过；已目视检查 390×844 下的满值、半血、蓄力、Combo 与空槽状态。未部署。
- 按反馈重做护盾：移除中心实心圆、圆环冲击波与格挡时的怪物白色剪影，常态改为铺满画布的半透明玻璃护罩和边缘反光；受击只显示从碰撞中心向外扩散的细白裂纹，护盾耗尽时生成半透明玻璃碎片，格挡时不再叠加整屏闪光。药剂槽和玻璃护盾全部改动已提交并推送，Service Worker 更新为 `v32`，部署到 `http://64.83.41.39:18088`；部署目录与 `dist/` 完全一致，Nginx 校验和重载通过，公开端点完整 Chromium 流程通过。22 项逻辑测试、生产构建、依赖审计、项目本地 Playwright 客户端和本地 Chromium/Firefox/WebKit 完整流程均通过，并已目视检查 390×844 下护盾完整、受击、裂纹退场和碎裂状态。
- 重排战斗 HUD：玩家状态置顶并改为生命、护盾、能量（当前盘面目标完成度）三条药剂槽，怪物状态独立为第二行且名称几何居中；删除战数、敌方伤害/架势、得分、攻击、下方护盾值和失误次数，目标操作提示与“剩余 N”拆分为怪物 HUD 下方的独立提示胶囊。护盾状态新增真实 `maxShield` 容量用于 HUD 比例，Service Worker 缓存准备更新为 `v33`。23 项逻辑测试、构建、依赖审计、项目本地 Playwright 客户端及 Chromium/Firefox/WebKit 完整五战流程通过；已目视检查 390×844 的开局、Combo、护盾受击和 Boss 布局，三层 HUD 无重叠，怪名中心误差为 0。本轮未部署。
- 移除正确与错误点击中的邻居波动缩放：点击反馈现在只作用于被点击泡泡，其他泡泡保持静止；保留当前泡泡的按压、弹破、粒子、数字和错误摇晃。生产 E2E 新增正确/错误点击期间非点击泡泡零形变断言和独立点击截图；上一轮 HUD 与本轮改动已提交并推送，Service Worker 更新为 `v34`，部署到 `http://64.83.41.39:18088`。23 项逻辑测试、本地 Chromium/Firefox/WebKit 与公开地址 Chromium 完整五战流程通过，部署目录和 `dist/` 完全一致。
